{{!-- Framework chrome: theme control only (no second navbar).
     Fixed + high z-index so it stays clickable above Agency fixed-top (#mainNav ~1030). --}}
<div class="thalia-theme-bar" aria-label="Theme">
  {{> theme-toggle palette=true }}
</div>
