<div class="box">
  <h2>First-time setup</h2>
  {{#if error}}
  <p class="error">{{error}}</p>
  {{/if}}
  <p>Use this page to create the first admin user when the site has no admins yet.</p>
  <p><a href="/newUser">Create new user</a> — if you are the first user, consider using an admin role (or run setup from the server).</p>
</div>
