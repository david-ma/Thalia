import type { RawWebsiteConfig } from 'thalia/types'

/**
 * Static visual reference for the upstream Start Bootstrap Agency theme.
 *
 * Keeping a real Thalia config makes `bun dev example-agency` work while all
 * content and assets intentionally remain under public/ for baseline comparison.
 */
export const config: RawWebsiteConfig = {}
