// Same request path as framework src/js/utils.ts: this intentionally proves
// that a site may replace a framework browser module from its own source tree.
export const sourceOwner = 'example-src-site-override'
